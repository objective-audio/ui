language: objective-c
osx_image: xcode10.1
branches:
  only:
    - master
notifications:
  email: false
install:
  - brew install clang-format
script:
  - git submodule update --init --recursive
  - xcodebuild -workspace ui_mac.xcworkspace -scheme ui_mac_tests -configuration Debug OBJROOT=build -sdk macosx clean test
